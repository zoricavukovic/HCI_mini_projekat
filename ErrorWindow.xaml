<Window x:Class="MiniProjekatHCI.ErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MiniProjekatHCI"
        mc:Ignorable="d" Title="Error" Height="200" Width="400" ResizeMode="NoResize" Icon="error.png">
    <Grid Background="#FFDCFFB3">
        <Grid.RowDefinitions >
            <RowDefinition Height="*"/>
            <RowDefinition Height="*" />
            
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Foreground="#FF2A4191" FontSize="14" x:Name="errorMessage" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,29,0,0" Grid.ColumnSpan="2">Cao</TextBlock>
        <Image Source="error.png" Grid.Row="1"></Image>
        <Button Content="OK"  Background="#FFD2F9A3" Click="Click_OK" Foreground="#FF2A4191" BorderBrush="#FF2A4191" BorderThickness="1" VerticalAlignment="Top" Margin="125,10,125,0" Grid.Row="1" Grid.ColumnSpan="2">
        </Button>

    </Grid>
</Window>
